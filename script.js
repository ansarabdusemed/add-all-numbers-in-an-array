// Declare an array
var myArr = [ 2, 3, 4, 5, 6];
// initialize variable to 0
var total = 0;
//iterate through the loop
for (var i = 0; i < myArr.length; i++) {
  //add the numbers in the array to total 
   total = total + myArr[i];
}

console.log(total)
